# URL del API REST (output `api_endpoint` de Terraform).
# Vacío → modo mock (CRUD en memoria) para poder probar el panel sin backend.
VITE_API_URL=

# En producción, además, los datos de Cognito (outputs de Terraform) para el login real:
# VITE_COGNITO_USER_POOL_ID=
# VITE_COGNITO_CLIENT_ID=
